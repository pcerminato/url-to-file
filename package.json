{
  "name": "url-to-file",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx --env-file=.env --watch src/server.ts",
    "dev:queue": "npm run build && node --env-file=.env dist/file-queue/receiver.js",
    "build": "rm -rf ./dist && tsc && cp -R ./src/ui ./dist && mkdir ./dist/static",
    "start": "node --env-file=.env dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^2.0.1",
    "express": "^5.2.1",
    "pg": "^8.21.0",
    "puppeteer": "^25.1.0",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "@types/pg": "^8.20.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
